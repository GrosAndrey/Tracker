//
//  Color.swift
//  Tracker
//
//  Created by Андрей Грошев on 28.04.2026.
//

import UIKit

enum DSColor {
    
    static var ypGreen: UIColor {
        guard let color = UIColor(named: "YP_green") else {
            assertionFailure("Color YP_green not found")
            return .systemGreen
        }
        return color
    }
    
    static var ypRed: UIColor {
        guard let color = UIColor(named: "YP_red") else {
            assertionFailure("Color YP_red not found")
            return .systemRed
        }
        return color
    }
    
    static var ypBlue: UIColor {
        guard let color = UIColor(named: "YP_blue") else {
            assertionFailure("Color YP_blue not found")
            return .blue
        }
        return color
    }
    
    static var ypWhite: UIColor {
        guard let color = UIColor(named: "YP_white") else {
            assertionFailure("Color YP_white not found")
            return .white
        }
        return color
    }
    
    static var ypBlack: UIColor {
        guard let color = UIColor(named: "YP_black") else {
            assertionFailure("Color YP_black not found")
            return .black
        }
        return color
    }
    
    static var ypGray: UIColor {
        guard let color = UIColor(named: "YP_gray") else {
            assertionFailure("Color YP_gray not found")
            return .gray
        }
        return color
    }
    
    static var ypSearchBackGround: UIColor {
        guard let color = UIColor(named: "YP_searchBackGround") else {
            assertionFailure("Color YP_searchBackGround not found")
            return .gray
        }
        return color
    }
    
    static var ypLigtGray: UIColor {
        guard let color = UIColor(named: "YP_ligtGray") else {
            assertionFailure("Color YP_ligtGray not found")
            return .lightGray
        }
        return color
    }
    
    static var ypBackgroundGray: UIColor {
        guard let color = UIColor(named: "YP_backgroundGray") else {
            assertionFailure("Color YP_backgroundGray not found")
            return .systemGray
        }
        return color
    }
    
    static var ypSelection1: UIColor {
        guard let color = UIColor(named: "YP_selection1") else {
            assertionFailure("Color YP_selection1 not found")
            return .systemRed
        }
        return color
    }
    
    static var ypSelection2: UIColor {
        guard let color = UIColor(named: "YP_selection2") else {
            assertionFailure("Color YP_selection2 not found")
            return .systemGray
        }
        return color
    }
    
    static var ypSelection3: UIColor {
        guard let color = UIColor(named: "YP_selection3") else {
            assertionFailure("Color YP_selection3 not found")
            return .systemGray
        }
        return color
    }
    
    static var ypSelection4: UIColor {
        guard let color = UIColor(named: "YP_selection4") else {
            assertionFailure("Color YP_selection4 not found")
            return .systemGray
        }
        return color
    }
    
    static var ypSelection5: UIColor {
        guard let color = UIColor(named: "YP_selection5") else {
            assertionFailure("Color YP_selection5 not found")
            return .systemGray
        }
        return color
    }
    
    static var ypSelection6: UIColor {
        guard let color = UIColor(named: "YP_selection6") else {
            assertionFailure("Color YP_selection6 not found")
            return .systemGray
        }
        return color
    }
    
    static var ypSelection7: UIColor {
        guard let color = UIColor(named: "YP_selection7") else {
            assertionFailure("Color YP_selection7 not found")
            return .systemGray
        }
        return color
    }
    
    static var ypSelection8: UIColor {
        guard let color = UIColor(named: "YP_selection8") else {
            assertionFailure("Color YP_selection8 not found")
            return .systemGray
        }
        return color
    }
    
    static var ypSelection9: UIColor {
        guard let color = UIColor(named: "YP_selection9") else {
            assertionFailure("Color YP_selection9 not found")
            return .systemGray
        }
        return color
    }
    
    static var ypSelection10: UIColor {
        guard let color = UIColor(named: "YP_selection10") else {
            assertionFailure("Color YP_selection10 not found")
            return .systemGray
        }
        return color
    }
    
    static var ypSelection11: UIColor {
        guard let color = UIColor(named: "YP_selection11") else {
            assertionFailure("Color YP_selection11 not found")
            return .systemGray
        }
        return color
    }
    
    static var ypSelection12: UIColor {
        guard let color = UIColor(named: "YP_selection12") else {
            assertionFailure("Color YP_selection12 not found")
            return .systemGray
        }
        return color
    }
    
    static var ypSelection13: UIColor {
        guard let color = UIColor(named: "YP_selection13") else {
            assertionFailure("Color YP_selection13 not found")
            return .systemGray
        }
        return color
    }
    
    static var ypSelection14: UIColor {
        guard let color = UIColor(named: "YP_selection14") else {
            assertionFailure("Color YP_selection14 not found")
            return .systemGray
        }
        return color
    }
    
    static var ypSelection15: UIColor {
        guard let color = UIColor(named: "YP_selection15") else {
            assertionFailure("Color YP_selection15 not found")
            return .systemGray
        }
        return color
    }
    
    static var ypSelection16: UIColor {
        guard let color = UIColor(named: "YP_selection16") else {
            assertionFailure("Color YP_selection16 not found")
            return .systemGray
        }
        return color
    }
    
    static var ypSelection17: UIColor {
        guard let color = UIColor(named: "YP_selection17") else {
            assertionFailure("Color YP_selection17 not found")
            return .systemGray
        }
        return color
    }
    
    static var ypSelection18: UIColor {
        guard let color = UIColor(named: "YP_selection18") else {
            assertionFailure("Color YP_selection18 not found")
            return .systemGray
        }
        return color
    }
}
